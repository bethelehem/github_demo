/* globals describe, it, expect */
describe('filter', function () {
  it('filter stateCode', function () {
    function filter (StateCode) {
      return NC
    }

    expect("data.stateCode.DC").toBe(DC)
  })
})
